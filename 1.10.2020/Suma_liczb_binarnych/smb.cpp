#include <bits/stdc++.h>
using namespace std;
int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(NULL);
    cout.tie(NULL);
    long long n;
    cin>>n;
    for(int i=0;i<n;i++) cout<<"1";
    for(int i=1;i<n;i++) cout<<"0";
}
